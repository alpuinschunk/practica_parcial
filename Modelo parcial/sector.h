/*#ifndef SECTOR_INCLUDE_H
#define SECTOR_INCLUDE_H

typedef struct
{
    int idSector;
    char descripcion[20];
    int isEmpty;
} eSector;


int elegirSector(eSector sectores[], int len);
void cargarDescripcion(eSector sectores[], int tamSector, int idSector, char cadena[]);

#endif // SECTOR_INCLUDE_*/
